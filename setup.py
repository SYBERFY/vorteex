from setuptools import setup, find_packages

setup(
    name='vorteex',
    version='0.0.1',
    author='Syberfy',
    description='CLI tool for Vorteex',
    packages=find_packages(),
    install_requires=[
    ],
    python_requires='>=3.7',
)
